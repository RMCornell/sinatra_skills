<section>
   <h1>Apparently this is everything you know</h1>

  <% @skills.each do |skill| %>
  <div>
    <h3><a href="/skills/<%= skill.id %>"><%= skill.name %></a></h3>
  </div>
      <% end %>

</section>
